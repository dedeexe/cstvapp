import SwiftUI

enum Typography {
    static let captionSmall = Font.system(size: 10)
    static let captionTiny = Font.system(size: 8)
    static let subtitle = Font.system(size: 12)
    static let caption = Font.system(size: 14)
}
